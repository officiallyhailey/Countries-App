# Mapbox public access token for the saved countries map.
# Must be a public token (starts with pk.). Secret sk. tokens are inlined into the
# frontend bundle by Vite and would be readable by anyone loading the site.
# Copy this file to .env.local, which is gitignored by the *.local rule.
VITE_MAPBOX_TOKEN=
