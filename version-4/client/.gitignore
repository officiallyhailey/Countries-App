# installed packages and the vite build output, both get rebuilt from source so neither belongs in the repo.
# netlify runs npm install and npm run build itself on every deploy, so a committed copy would only go stale
node_modules
dist

# vite reads these for local-only settings and they can end up holding keys
*.local

# npm writes these when an install fails
*.log
