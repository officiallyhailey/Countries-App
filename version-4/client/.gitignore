# both of these come back on their own, npm install rebuilds node_modules and netlify runs the build itself, so a copy in here would just go stale
node_modules
dist

# vite picks these up for local settings and they can end up with keys in them
*.local

# npm only writes these when an install fails
*.log

# mac drops these in every folder I've opened
.DS_Store
